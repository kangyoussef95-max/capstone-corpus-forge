# Purpose: Persisted quiz sets generated by the quiz AI workflow.
# Each entry records the source document(s), generation date, prompt steering parameters used,
# and the resulting questions with answer options and correct answers.
# Populated by ai_workflows.generate_quiz() and saved by persistence.py.
